#!/bin/sh

echo "🔨 Running pre-commit checks..."

# TODO: Re-enable after fixing EnhancedPerformanceService.ts and ZeroTrustSecurityService.ts
# These files have corrupted newline characters that need manual fixing
# Run TypeScript compilation check
# echo "📝 Type checking..."
# bun run typecheck || exit 1

# Run lint-staged (ESLint, Prettier, Jest on staged files)
echo "🧹 Linting and formatting staged files..."
bun run lint:staged || exit 1

echo "✅ Pre-commit checks passed!"
